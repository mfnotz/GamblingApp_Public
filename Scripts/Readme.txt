- Don't run the Create scripts unless the VS migration doesn't work!
- Don't run the Create scripts unless the VS migration doesn't work!
- Don't run the Create scripts unless the VS migration doesn't work!

- Run the Adding User Script modifying it with your Windows user.